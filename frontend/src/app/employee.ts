//An inerface to mirror all the request payload we pass to the backend
//Part of typescript
export interface Employee {
  id: number;
  name: string;
  email: string;
  jobTitle: string;
  phone: string;
  imageUrl: string;
  employeeCode: string;
}
